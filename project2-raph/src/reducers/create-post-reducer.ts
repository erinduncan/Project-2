import { }from ".";
import {} from "../action-mappers/create-post-actions";


const initialState:ICreatePostState = {
    
}

export const createPostReducer = (state = initialState, action:any) => {
    switch (action.type) {
        // case : {
        //     return {
        //         ...state,
        //     }
        // }
        // case : {
        //     return {
        //         ...state,
        //     }
        // }

        default:
            return state;
    }
}